# First rule starts the chain and contains the metadata/disruptive actions
SecRule REQUEST_URI "@beginsWith /api/admin" "phase:2,id:100001,deny,status:403,log,msg:'Unauthorized Admin Access Attempt',chain"
    # Second rule must match for the chain to execute
    SecRule REQUEST_HEADERS:User-Agent "@contains MaliciousScanner" "t:lowercase"